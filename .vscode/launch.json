{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Pacman in Chrome",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:8080/index.html",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Serve Pacman (Python)"
    }
  ]
}
